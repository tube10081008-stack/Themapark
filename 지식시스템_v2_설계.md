# 지식 시스템 v2 — 영상 방법론의 재설계

> 원본: 옵시디언 + 클로드 코드로 Raw → Wiki 필터링 시스템을 만드는 영상.
> 뼈대(원칙이 필터를 만들고, 필터가 정보를 거른다)는 맞다. 고치는 것은 두 가지 —
> 정보가 시간이 지나면 썩는다는 사실을 다루지 않는다는 점, 그리고 슬래시 커맨드 세 개를
> 전제로 설계됐다는 점.

## 1. 진단 — v1이 6개월 뒤 무너지는 다섯 지점

**(1) 정보에 유효기간이 없다.**
경쟁시설 요금표, 봉화군 조례, 은어축제 방문객 수 — 전부 6개월이면 낡는다. v1의 위키는 한 번
들어온 문장을 영원히 참으로 취급한다. 가장 위험한 실패는 위키가 비어 있는 게 아니라 **낡은
것을 확신 있게 말하는 것**이다.

**(2) 필터가 이진(binary)이다.**
통과/탈락만 있고 "얼마나 믿을 만한가"가 없다. 직접 전화해서 확인한 요금과 블로그에서 본
요금이 같은 무게로 저장된다. 나중에 둘이 충돌해도 어느 쪽을 믿을지 판단할 근거가 남지 않는다.

**(3) 위키가 커질수록 `/query`가 나빠진다.**
전체를 컨텍스트에 밀어넣는 구조라 문서가 수백 개를 넘으면 관련 없는 내용이 답을 희석시킨다.
지식이 쌓일수록 좋아져야 하는 시스템이 반대로 간다.

**(4) 아는 것과 정한 것이 섞여 있다.**
"봉화군 인구는 3만 명이다"(사실)와 "그래서 로컬 수요만으로는 안 되니 광역 상권으로 간다"(판단)가
같은 폴더에 있다. 사업에서 재사용 가치가 가장 높은 것은 사실이 아니라 **과거의 판단과 그때의
근거**다. 6개월 뒤 "왜 그렇게 정했더라"에 답할 수 없으면 같은 논의를 처음부터 다시 한다.

**(5) 지식이 행동으로 이어지지 않는다.**
위키는 읽히기만 한다. 반대로 이 저장소의 `agents/` 11종은 실행만 있고 근거가 없다 —
`sales_agent.py`는 경쟁시설 요금을 모른 채 제안서를 쓴다. 둘이 붙어 있지 않으면 둘 다 반쪽이다.

## 2. 재설계 — 2계층에서 4계층으로

```
principles/   ← 판정 기준. 가장 작고, 가장 안 변하고, 가장 중요하다
raw/          ← 원본. 손대지 않는다. 출처와 수집일만 붙인다
wiki/         ← 아는 것. 사실 + 신뢰도 + 유효기간
decisions/    ← 정한 것. 판단 + 근거 + 그때 몰랐던 것
```

v1의 raw/wiki에 위아래로 한 층씩 붙었다. 위는 **원칙**(필터의 근거), 아래는 **결정 로그**(필터의 산출물).

### principles/

파일 3~5개, 각 한 페이지를 넘기지 않는다. 여기 없는 기준으로는 위키 편입을 판정하지 않는다.
원칙이 늘어나면 시스템이 아니라 잡동사니가 된다.

### raw/

편집 금지. 요약도 금지. 원본 그대로 두고 frontmatter만 붙인다. 나중에 판단이 바뀌면 원본으로
되돌아가야 하는데, 요약해두면 되돌아갈 데가 없다.

### wiki/

모든 문서에 다음 frontmatter를 강제한다.

```yaml
---
source: raw/2026-08-20_봉화군_공고_제2026-118호.pdf
confidence: high        # high(1차 출처·직접 확인) | medium(2차 보도) | low(추정·전언)
verified: 2026-08-20
expires: 2027-08-20     # 이 날짜 이후에는 '모름'으로 취급
supersedes: wiki/시장/경쟁시설_요금_2025.md
---
```

`expires`가 핵심이다. 지난 문서는 삭제되지 않고 **'만료' 상태로 남는다** — 답변에 인용될 때
반드시 "2026-08 기준, 만료됨"이 함께 나온다. 삭제하면 그런 것이 있었다는 사실까지 잃는다.

### decisions/

한 결정에 한 파일. 형식은 네 항목이면 충분하다.

```markdown
## 결정
평일 비수기 목표를 로컬 유입이 아니라 단체 유치로 채운다.

## 근거
wiki/시장/인구.md — 봉화군 유아·초등 인구로는 평일 24명이 나오지 않는다
wiki/시장/광역상권.md — 안동·영주 1시간권 어린이집·유치원 목록

## 그때 몰랐던 것
단체 단가, 차량 지원 여부, 경쟁 유치시설의 기존 계약 관계

## 재검토 조건
단체 문의가 3개월간 월 2건 미만이면 이 결정을 다시 연다
```

"그때 몰랐던 것"과 "재검토 조건"이 이 파일의 값어치다. 사업 판단은 대부분 불충분한 정보로
내리는데, **무엇이 불충분했는지를 적어두면 그것이 다음 `/ingest`의 우선순위가 된다.**

## 3. 실행 구조 — 슬래시 커맨드에서 스킬·서브에이전트·훅으로

v1은 명령 세 개다. 명령은 사람이 매번 쳐야 하고, 칠 때마다 전체 프롬프트가 컨텍스트에 올라간다.
셋을 각자 맞는 형태로 나눈다.

### /ingest → 서브에이전트로 돌린다

raw 원본을 통째로 읽는 것은 컨텍스트를 가장 많이 태우는 작업이고, 그 부스러기가 메인 대화에
남으면 이후 모든 답이 나빠진다. **서브에이전트는 자기 컨텍스트에서 읽고 결과만 돌려준다** —
v1이 구조적으로 못 하던 것이다.

2단으로 나눈다.

| 단계 | 하는 일 | 모델 | 왜 |
|---|---|---|---|
| 1차 분류 | 이 문서가 어느 원칙에 걸리는가, 버릴 것인가 | Haiku 4.5 ($1/$5 per MTok) | 분류는 판단이 아니다. raw는 양이 많고 대부분 버려진다 |
| 2차 판정 | 위키 편입, 신뢰도·유효기간 부여, 기존 문서와의 충돌 판정 | Opus 5 ($5/$25) | 여기가 시스템의 품질을 결정한다. 아끼면 안 되는 유일한 지점 |

버려지는 문서가 대부분이므로 1차를 Haiku로 거르면 실질 비용이 크게 내려간다.

### /query → 스킬로 만든다 (점진적 공개)

스킬은 진입 파일만 먼저 읽히고 필요한 하위 파일을 그때 읽는다. 위키 전체가 아니라
**인덱스 → 해당 분류 → 개별 문서** 순으로 좁혀 들어가므로 위키가 커져도 답이 희석되지 않는다.
v1의 "위키가 클수록 나빠진다" 문제가 여기서 풀린다.

답변 규칙 두 개를 스킬에 박아둔다.

- 인용한 위키 문서의 `verified` 날짜와 `confidence`를 항상 함께 낸다
- 만료된 문서만으로 답이 만들어지면 답 대신 **"이건 다시 확인해야 한다"**를 낸다

### /lint → 3종으로 확장하고, 훅으로 자동화한다

v1의 lint는 문법·구조 검사다. 실제로 시스템을 죽이는 것은 그것이 아니다.

| 검사 | 무엇을 잡는가 |
|---|---|
| 형식 | frontmatter 누락, 깨진 내부 링크, 고아 문서 |
| 모순 | 같은 주제의 두 문서가 다른 숫자를 말하는 경우 |
| 부패 | `expires` 경과, `source` 링크 사망, `confidence: low`가 6개월째 그대로인 것 |

모순 검사가 가장 값어치가 크고, v1에는 없다. 그리고 세션이 열릴 때 훅으로 미처리 raw 개수와
만료 임박 문서를 자동 보고하면 사람이 lint를 기억할 필요가 없어진다.
**기억해야 돌아가는 시스템은 돌아가지 않는다.**

### 원칙은 CLAUDE.md가 물고 있는다

`CLAUDE.md`에서 `principles/`를 참조하면 모든 세션이 원칙을 자동으로 로드한다. v1처럼 프롬프트에
암묵적으로 들어 있는 것이 아니라 **버전 관리되는 파일**로 존재한다. 원칙이 바뀌면 git log에
남고, 왜 바뀌었는지는 `decisions/`에 남는다.

## 4. 비용 설계

프롬프트 캐싱은 프리픽스 매칭이다 — 앞쪽이 한 바이트라도 바뀌면 뒤가 전부 무효화된다.
그래서 **고정된 것을 앞에, 변하는 것을 뒤에** 둔다.

```
[원칙 전문] → [위키 인덱스] → (캐시 경계) → [오늘의 질문]
   안 변함        하루 단위                     매번 변함
```

원칙과 인덱스가 캐시에 얹히면 매 질의의 실질 입력 비용이 크게 떨어진다. 반대로 시스템
프롬프트에 현재 시각 같은 것을 넣으면 캐시가 매번 깨진다. `usage.cache_read_input_tokens`가
계속 0이면 어딘가에 그런 무효화 요인이 있다는 뜻이다.

서브에이전트는 effort를 낮게 둔다 — ingest 1차 분류처럼 판단 폭이 좁은 일에 깊은 추론은 낭비다.

## 5. 원칙 파일 쓰는 법 — 이 시스템의 전부

나머지가 전부 완벽해도 원칙이 추상적이면 AI 사서는 일반론 요약기가 된다.
기준은 하나다. **문서 하나를 놓고 통과/탈락을 판정할 수 있는가.**

| 판정 불가 | 판정 가능 |
|---|---|
| 지역 상생에 기여한다 | 동일 조건이면 봉화군 소재 업체와 계약한다 |
| 안전을 최우선으로 한다 | 계약·법령상 의무가 걸린 사항은 `confidence: high`가 아니면 위키에 넣지 않는다 |
| 데이터 기반으로 판단한다 | 손익 관련 숫자는 1차 출처 없이 인용하지 않는다 |

오른쪽 문장들은 이 저장소에 이미 있다. `agents/README.md`의 "계약 준수가 걸린 계산을 모델
판단에 맡기지 않는다", `cashflow_agent.py`의 "손익과 현금은 다르다" — **이미 원칙을 쓰고
있었고 코드 주석에 흩어져 있을 뿐이다.** 첫 작업은 새로 쓰는 것이 아니라 모으는 것이다.

## 6. 봉뜨락 적용

현재 저장소는 이미 절반이 있다.

| 지금 있는 것 | v2에서의 자리 |
|---|---|
| `봉화_리틀포레스트봉뜨락_운영계획서_초안.md` | `wiki/`의 초기 내용 — 주제별로 분해해야 함 |
| `agents/README.md`의 "왜 필요한가" 열 | `principles/`의 원재료 |
| `agents/` 11종 | `wiki/`·`decisions/`를 읽고 움직이는 실행층 |
| (없음) | `raw/` — 여기가 비어 있다 |
| (없음) | `decisions/` — 운영계획서에 판단이 사실과 섞여 있다 |

운영계획서 안에 이미 표시가 되어 있다. `Ⅱ-7. 확인이 더 필요한 사항`,
`Ⅵ. 요금 정책(예시안 — 확정 전 시장조사 필요)`, `ⅩⅠ. 수지분석(예시 — 반드시 실제 데이터로 교체)`.
**이 세 곳이 `/ingest`가 처음 채워야 할 빈칸이고, 동시에 첫 `decisions/` 파일의
"그때 몰랐던 것"이다.**

### 순서

1. `principles/` 먼저. `agents/README.md`와 운영계획서에서 판정 가능한 문장만 뽑아 3~5개.
2. 운영계획서를 `wiki/`로 분해. 이때 판단 문장은 `decisions/`로 분리한다.
   `Ⅱ-8 가설(수정)`은 그 자체로 첫 결정 파일이다.
3. `raw/`를 만들고 세 빈칸을 채울 자료부터 넣는다 — 경쟁시설 요금, 은어축제 통계,
   안동·영주 유치원 목록.
4. `/ingest`·`/query`·`/lint` 구축.
5. `agents/`가 `wiki/`를 읽도록 연결. `sales_agent.py`부터.

1~2번은 하루면 되고 그것만으로도 지금보다 낫다. 3번부터가 진짜 시스템이다.

## 7. 이 설계가 실패하는 조건

v2도 다음 셋 중 하나가 걸리면 v1과 똑같이 무너진다.

- **원칙이 3개를 넘어 10개가 되면.** 판정이 느려지고, 느려지면 안 하게 된다.
- **`decisions/`를 안 쓰면.** 결정 로그는 쓸 때 귀찮고 6개월 뒤에야 값어치가 나온다.
  이 시차를 못 견디면 그냥 위키만 남는다.
- **lint를 사람이 기억해야 하면.** 훅으로 자동화하지 않으면 두 달 안에 멈춘다.

그리고 v1이 약속한 "지리적·시간적 자유"는 이 설계도 약속하지 않는다. 위키가 커지면 lint
부담이 늘고 유지 비용은 0으로 가지 않는다. 이 시스템이 실제로 주는 것은 자유가 아니라
**판단의 재사용**이다 — 같은 고민을 두 번 하지 않는 것. 5명 조직에서는 그쪽이 더 크다.
